diff --git a/src/test/tableview_test.cpp b/src/test/tableview_test.cpp
index 34f638a..704c3f1 100644
--- a/src/test/tableview_test.cpp
+++ b/src/test/tableview_test.cpp
@@ -3,10 +3,36 @@
 #include <gtest/gtest.h>
 #include <gmock/gmock.h>
 
+#include <QSortFilterProxyModel>
+#include <QStandardItemModel>
 #include <QtDebug>
 #include "proto/headers.pb.h"
+#include "widget/wlibrarytableview.h"
 #include "widget/wtracktableviewheader.h"
 
+namespace {
+
+class TestLibraryTableView final : public WLibraryTableView {
+  public:
+    TestLibraryTableView()
+            : WLibraryTableView(nullptr, UserSettingsPointer()) {
+    }
+
+    void onShow() override {
+    }
+
+    bool hasFocus() const override {
+        return QWidget::hasFocus();
+    }
+
+  protected:
+    QString getModelStateKey() const override {
+        return QStringLiteral("test-model");
+    }
+};
+
+} // namespace
+
 class HeaderViewStateTest : public testing::Test {
 };
 
@@ -69,3 +95,36 @@ TEST_F(HeaderViewStateTest, BadHeaderState) {
     HeaderViewState view_state("BLAHBLAHBLAHBAD");
     ASSERT_FALSE(view_state.healthy());
 }
+
+TEST(LibraryTableViewStateTest, RestoreAfterProxyResetUsesFreshIndexes) {
+    QStandardItemModel sourceModel(3, 2);
+    QSortFilterProxyModel proxyModel;
+    proxyModel.setSourceModel(&sourceModel);
+
+    TestLibraryTableView tableView;
+    tableView.setModel(&proxyModel);
+
+    const QModelIndex selectedIndex = proxyModel.index(1, 0);
+    const QModelIndex currentIndex = proxyModel.index(1, 1);
+    tableView.selectionModel()->select(selectedIndex,
+            QItemSelectionModel::Select | QItemSelectionModel::Rows);
+    tableView.selectionModel()->setCurrentIndex(
+            currentIndex, QItemSelectionModel::NoUpdate);
+    tableView.saveTrackModelState(&proxyModel, QStringLiteral("usb-device"));
+
+    // Rebuilding a Rekordbox USB model invalidates the proxy's internal
+    // indexes, even if the proxy object and state key are reused.
+    sourceModel.clear();
+    sourceModel.setColumnCount(2);
+    sourceModel.setRowCount(4);
+
+    ASSERT_TRUE(tableView.restoreTrackModelState(
+            &proxyModel, QStringLiteral("usb-device")));
+    const QModelIndexList restoredRows = tableView.selectionModel()->selectedRows();
+    ASSERT_EQ(1, restoredRows.size());
+    EXPECT_EQ(1, restoredRows.first().row());
+    EXPECT_EQ(&proxyModel, restoredRows.first().model());
+    EXPECT_EQ(1, tableView.currentIndex().row());
+    EXPECT_EQ(1, tableView.currentIndex().column());
+    EXPECT_EQ(&proxyModel, tableView.currentIndex().model());
+}
diff --git a/src/widget/wlibrarytableview.cpp b/src/widget/wlibrarytableview.cpp
index aab492c..564d4ac 100644
--- a/src/widget/wlibrarytableview.cpp
+++ b/src/widget/wlibrarytableview.cpp
@@ -1,5 +1,7 @@
 #include "widget/wlibrarytableview.h"
 
+#include <algorithm>
+
 #include <QApplication>
 #include <QFocusEvent>
 #include <QFontMetrics>
@@ -69,6 +71,11 @@ void WLibraryTableView::saveTrackModelState(
     VERIFY_OR_DEBUG_ASSERT(!key.isEmpty()) {
         return;
     }
+    QItemSelectionModel* pSelectionModel = selectionModel();
+    VERIFY_OR_DEBUG_ASSERT(this->model() == model && pSelectionModel &&
+            pSelectionModel->model() == model) {
+        return;
+    }
     ModelState* state = m_modelStateCache.take(key);
     if (!state) {
         state = new ModelState();
@@ -77,13 +84,26 @@ void WLibraryTableView::saveTrackModelState(
     state->verticalScrollPosition = verticalScrollBar()->value();
     state->horizontalScrollPosition = horizontalScrollBar()->value();
 
-    state->selectedRows = selectionModel()->selectedRows();
+    state->selectedRows.clear();
+    const QModelIndexList selectedRows = pSelectionModel->selectedRows();
+    state->selectedRows.reserve(selectedRows.size());
+    for (const QModelIndex& index : selectedRows) {
+        if (index.isValid() && index.model() == model) {
+            state->selectedRows.append(index.row());
+        }
+    }
+    std::sort(state->selectedRows.begin(), state->selectedRows.end());
+    state->selectedRows.erase(
+            std::unique(state->selectedRows.begin(), state->selectedRows.end()),
+            state->selectedRows.end());
 
-    const QModelIndex currIndex = selectionModel()->currentIndex();
+    const QModelIndex currIndex = pSelectionModel->currentIndex();
     if (currIndex.isValid()) {
-        state->currentIndex = currIndex;
+        state->currentRow = currIndex.row();
+        state->currentColumn = currIndex.column();
     } else {
-        state->currentIndex = QModelIndex();
+        state->currentRow = -1;
+        state->currentColumn = -1;
     }
 
     m_modelStateCache.insert(key, state, 1);
@@ -93,7 +113,12 @@ bool WLibraryTableView::restoreTrackModelState(
         const QAbstractItemModel* model, const QString& key) {
     //qDebug() << "restoreTrackModelState:" << model << key;
     //qDebug() << m_modelStateCache.keys();
-    if (model == nullptr) {
+    if (model == nullptr || model != this->model()) {
+        return false;
+    }
+
+    QItemSelectionModel* pSelectionModel = selectionModel();
+    if (!pSelectionModel || pSelectionModel->model() != model) {
         return false;
     }
 
@@ -112,16 +137,22 @@ bool WLibraryTableView::restoreTrackModelState(
 
     // Build a selection range rather than selecting each track individually,
     // which can lag the GUI by spamming selectionChanged() handlers.
-    QItemSelectionModel* pSelectionModel = selectionModel();
     pSelectionModel->clearSelection();
     QItemSelection newSelection;
-    QModelIndexList selectedRows = state->selectedRows;
     QModelIndex topLeft;
     QModelIndex bottomRight;
-    if (!selectedRows.isEmpty()) {
-        // In saveTrackModelState() we fill state->selectedRows with the sorted
-        // rows, hence no need to sort here.
-        for (const QModelIndex& index : std::as_const(selectedRows)) {
+    if (!state->selectedRows.isEmpty() && model->columnCount() > 0) {
+        // Rebuild indexes from the active model. Cached QModelIndex instances
+        // may still look valid after a proxy/source model reset while pointing
+        // to deleted internal data.
+        for (int row : std::as_const(state->selectedRows)) {
+            if (row < 0 || row >= model->rowCount()) {
+                continue;
+            }
+            const QModelIndex index = model->index(row, 0);
+            if (!index.isValid()) {
+                continue;
+            }
             if (!topLeft.isValid()) {
                 // start new range. only done once for first row
                 topLeft = index;
@@ -143,14 +174,20 @@ bool WLibraryTableView::restoreTrackModelState(
         }
 
         // If we reached end, submit the last selection
-        if (bottomRight == selectedRows.last()) {
+        if (topLeft.isValid()) {
             newSelection.select(topLeft, bottomRight);
         }
     }
     pSelectionModel->select(newSelection,
             QItemSelectionModel::Select | QItemSelectionModel::Rows);
 
-    QModelIndex currIndex = state->currentIndex;
+    QModelIndex currIndex;
+    if (state->currentRow >= 0 && state->currentColumn >= 0 &&
+            model->rowCount() > 0 && model->columnCount() > 0) {
+        const int row = std::min(state->currentRow, model->rowCount() - 1);
+        const int column = std::min(state->currentColumn, model->columnCount() - 1);
+        currIndex = model->index(row, column);
+    }
     restoreCurrentIndex(currIndex);
 
     // reinsert the state into the cache
@@ -174,7 +211,7 @@ void WLibraryTableView::saveCurrentIndex() {
 
 void WLibraryTableView::restoreCurrentIndex(const QModelIndex& index) {
     QItemSelectionModel* pSelectionModel = selectionModel();
-    if (!pSelectionModel) {
+    if (!pSelectionModel || !model()) {
         return;
     }
     int row = index.isValid() ? index.row() : m_prevRow;
diff --git a/src/widget/wlibrarytableview.h b/src/widget/wlibrarytableview.h
index 6a63ecc..e66dbe7 100644
--- a/src/widget/wlibrarytableview.h
+++ b/src/widget/wlibrarytableview.h
@@ -3,6 +3,7 @@
 #include <QCache>
 #include <QString>
 #include <QTableView>
+#include <QVector>
 
 #include "library/library_decl.h"
 #include "library/libraryview.h"
@@ -15,10 +16,11 @@ class WLibraryTableView : public QTableView, public virtual LibraryView {
     Q_OBJECT
 
     struct ModelState {
-        int horizontalScrollPosition;
-        int verticalScrollPosition;
-        QModelIndexList selectedRows;
-        QModelIndex currentIndex;
+        int horizontalScrollPosition{0};
+        int verticalScrollPosition{0};
+        QVector<int> selectedRows;
+        int currentRow{-1};
+        int currentColumn{-1};
     };
 
   public:

